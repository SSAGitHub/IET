The template files are for reference only and are not used in the deployment.

Copy the template file e.g. RetroWorkFlowCleanUp.properties.template to RetroWorkFlowCleanUp.properties

Edit the properties file with the required values. These will be deployed to the target folder and will not be refreshed from GitHub. 